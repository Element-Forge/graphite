package io.github.graphite.gradle;

import org.gradle.api.Plugin;
import org.gradle.api.Project;

/**
 * Graphite Gradle Plugin - Generates type-safe GraphQL client code.
 *
 * <p>Apply this plugin to your Gradle project to generate GraphQL client code
 * from your schema during the build.
 */
public class GraphitePlugin implements Plugin<Project> {

    @Override
    public void apply(Project project) {
        // TODO: Implement plugin in issue #10
    }
}
